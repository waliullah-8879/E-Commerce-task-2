# Northstar API

Express/Mongoose API for products, authentication, checkout, and administration.

Run from the repository root with `npm install`, `npm install --prefix server`, then copy `.env.example` to `.env` and run `node src/seed.js` from `server`. `npm run server:dev` starts the API.
